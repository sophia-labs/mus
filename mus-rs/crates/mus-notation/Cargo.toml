[package]
name = "mus-notation"
version = "0.1.0"
description = "Parser for the MUS score notation — the effect-free half of the engine. Oracle-tested against the Python reference implementation."
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
regex = "1"

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
